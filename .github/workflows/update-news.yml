name: Debug Schedule Only

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  debug-schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Print debug message
        run: |
          echo "Scheduled run triggered at $(date -u) (UTC)"
